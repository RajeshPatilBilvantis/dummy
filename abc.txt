ValueError: Integer labels detected but original_classes not provided. Please provide original_classes when initializing the wrapper.
File <command-5386568783283269>, line 176
    166 # Create ensemble
    167 ensemble = VotingClassifier(
    168     estimators=[
    169         ('catboost', catboost_wrapper),
   (...)
    174     weights=[2, 1, 1]
    175 )
--> 176 ensemble.fit(X_train, train_df['second_product_category'])
    178 # Evaluate ensemble
    179 ensemble_pred = ensemble.predict(X_val)
File <command-5386568783283269>, line 103, in CatBoostWithCatFeatures.fit(self, X, y, eval_set, **kwargs)
     99         y = y_series.map(lambda x: self.original_classes[int(x)] if 0 <= int(x) < len(self.original_classes) else str(x)).values
    100     else:
    101         # Fallback: use integers as-is (but this won't work with string-based class_weights)
    102         # Better to raise an error
--> 103         raise ValueError("Integer labels detected but original_classes not provided. Please provide original_classes when initializing the wrapper.")
    104 else:
    105     # Original string labels - use as-is
    106     y = y_series.astype(str).values
